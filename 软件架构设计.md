# 车载视频透明数据图层生成软件架构设计说明书

> 文档用途：用于指导开发团队进行软件架构设计、模块拆分、接口定义、数据流实现、鲁棒性设计与安全边界控制。  
> 产品目标：构建一款桌面端 Python/Qt 软件，将 GPX/VBO 遥测数据解析、计算并渲染为可叠加到车载视频上的透明背景动态数据图层。  
> 架构重点：UI 要具备较好的交互与视觉质量；核心计时、分段、秒差、透明贴片渲染与导出必须可靠、准确、可验证、可维护。

---

## 1. 架构总原则

本项目不应按照“页面”组织代码，而应按照“数据流 + 领域能力 + 外部适配”组织架构。Qt 界面、GPX/VBO 解析、计时算法、图层组件、预览渲染、最终导出和 FFmpeg 调用必须解耦。

核心原则如下：

1. **UI 与核心算法彻底分离**  
   Qt 界面只负责显示、交互和用户命令收集，不直接承担 GPX/VBO 解析、圈速计算、分段计算、秒差计算、插值和 FFmpeg 命令拼接。

2. **计算数据与显示数据分离**  
   轨迹原始数据和计算坐标用于圈速、分段、距离、速度和秒差计算；显示坐标用于地图、卫星底图、轨迹视觉校准和小地图绘制。用户拖动轨迹对齐卫星图时，不允许直接修改原始遥测数据。

3. **预览渲染与最终导出分离**  
   预览可以低分辨率、低帧率、跳帧，以保证交互流畅；最终导出必须按照目标分辨率、目标帧率、目标透明通道格式重新渲染，不能复用低清预览结果。

4. **数据标准化优先**  
   GPX、VBO 等输入文件格式不同，但进入计算层前必须转换为统一的 `TelemetryStore` 标准遥测数据结构。后续所有算法和图层组件只能依赖标准数据，不直接依赖原始文件格式。

5. **真实值与估算值分离**  
   VBO 原始速度、原始横向 G、原始纵向 G、原始航向优先使用；GPX 缺失字段可以估算，但必须标记为估算值或低置信度。图层显示和导出时应保留数据质量标识。

6. **图层组件插件化**  
   赛道图、速度、G 力球、当前计时、秒差、分段计时、前后五圈表等都应作为独立图层组件实现。新增图标、仪表、曲线或自定义元素时，不应修改主渲染流程。

7. **数据流单向传递**  
   数据从导入、清洗、标准化、分析、插值、图层渲染、导出逐级向下流动。下游模块不得反向修改上游原始数据和核心计算结果。

8. **错误隔离与降级运行**  
   单个数据点异常、单个图层组件渲染失败、某个字段缺失、某个视频帧读取失败，不应导致整个软件崩溃。系统应尽量降级显示、记录日志并提示用户。

---

## 2. 总体架构

推荐采用 Clean Architecture / 六边形架构思想，按职责划分为七层：

```text
┌──────────────────────────────────────────────┐
│ 1. Presentation / UI Layer                    │
│ Qt 主窗口、地图编辑、画布编辑、同步面板、导出面板 │
└──────────────────────────────────────────────┘
                    ↓ Command / Query / Event
┌──────────────────────────────────────────────┐
│ 2. Application Service Layer                  │
│ 工程工作流、导入流程、同步流程、导出流程、状态编排 │
└──────────────────────────────────────────────┘
                    ↓ DTO / Domain API
┌──────────────────────────────────────────────┐
│ 3. Domain Core Layer                          │
│ 遥测数据模型、时间轴、轨迹、分段线、圈速、秒差、插值 │
└──────────────────────────────────────────────┘
                    ↓ Port Interface
┌──────────────────────────────────────────────┐
│ 4. Data Ingestion & Normalization Layer       │
│ GPX/VBO 解析、字段映射、清洗、质量报告、标准化数据表 │
└──────────────────────────────────────────────┘
                    ↓ Standard TelemetryStore
┌──────────────────────────────────────────────┐
│ 5. Overlay Widget Engine                      │
│ 赛道图、速度、G 力球、计时、秒差、分段、圈速表组件 │
└──────────────────────────────────────────────┘
                    ↓ Render Frame Request
┌──────────────────────────────────────────────┐
│ 6. Render / Preview / Export Layer            │
│ 低清预览、逐帧 RGBA 渲染、PNG 序列、MOV Alpha 输出 │
└──────────────────────────────────────────────┘
                    ↓ Adapter
┌──────────────────────────────────────────────┐
│ 7. Infrastructure Layer                       │
│ FFmpeg/FFprobe、文件系统、日志、缓存、地图瓦片、打包 │
└──────────────────────────────────────────────┘
```

依赖方向必须是单向的：

```text
UI → Application → Domain ← Infrastructure Adapter
                         ↑
                   Overlay Widget Engine
                         ↑
                  Render / Export Layer
```

关键要求：

- Domain Core 不依赖 Qt、FFmpeg、OpenCV、文件系统和具体 UI 控件。
- Application Service 可以调用 Domain Core 和 Infrastructure 接口，但不写具体计算算法。
- UI 层通过 ViewModel 或 Application Service 获取状态，不直接操作 DataFrame 和原始 parser。
- Render / Export 层通过标准 `TelemetryFrame` 获取某一帧所需数据，不重新解析文件、不重新计算圈速。

---

## 3. 分层职责说明

| 层级 | 核心职责 | 明确禁止 | 典型输出 |
|---|---|---|---|
| Presentation / UI Layer | 展示界面、响应拖拽、地图编辑、画布编辑、同步操作、导出配置 | 不做 GPX/VBO 解析；不做圈速/分段算法；不拼 FFmpeg 命令 | UserCommand、ViewModel、UIEvent |
| Application Service Layer | 编排完整用例：导入视频、导入遥测、设置赛道线、同步、预览、导出 | 不写底层算法；不直接绘图；不直接访问 UI 控件 | UseCaseResult、ProjectState |
| Domain Core Layer | 定义遥测模型、轨迹模型、时间轴、穿线检测、圈速、分段、秒差、插值 | 不依赖 Qt、FFmpeg、文件系统 | TelemetryStore、LapAnalysisResult、TelemetryFrame |
| Data Ingestion Layer | GPX/VBO 解析、字段映射、数据清洗、质量报告、标准化 | 不关心 UI 布局；不渲染图层；不导出视频 | RawTelemetryData、CleanTelemetryData、TelemetryStore |
| Overlay Widget Engine | 管理图层组件、绑定数据、执行单帧绘制 | 不重新计算圈速；不读取原始文件；不修改工程状态 | WidgetRenderResult |
| Render / Preview / Export Layer | 低清预览、全质量逐帧渲染、PNG 序列、Alpha 视频导出 | 不修改遥测数据；不改工程核心配置 | PNG 序列、MOV、WebM、导出 JSON |
| Infrastructure Layer | FFprobe、FFmpeg、文件读写、日志、缓存、地图瓦片、打包资源 | 不写业务规则 | 外部资源适配结果 |

---

## 4. 核心模块划分

### 4.1 Project Module：工程管理模块

负责管理工程文件、素材路径、图层布局、同步关系、导出配置、工程版本兼容和最近打开列表。

建议类：

```text
Project
ProjectState
ProjectRepository
ProjectSchemaValidator
RecentProjectManager
AssetLocator
AtomicProjectSaver
```

主要职责：

1. 新建工程、打开工程、保存工程、另存工程。
2. 记录视频路径、数据路径、同步偏移、起终点线、分段线、图层布局、样式主题和导出设置。
3. 素材路径失效时提示用户重新定位文件。
4. 保存工程时使用“临时文件写入 → 校验 → 原子替换”，避免工程文件损坏。
5. 工程文件必须带 `schema_version`，便于后续版本兼容。

工程文件建议结构：

```json
{
  "schema_version": "1.0",
  "project_name": "kart_overlay_project",
  "video": {},
  "telemetry": {},
  "track": {},
  "sync": {},
  "canvas": {},
  "widgets": [],
  "export": {},
  "quality_report": {}
}
```

边界要求：

- Project Module 不解析 GPX/VBO。
- Project Module 不执行计时计算。
- Project Module 不直接调用 FFmpeg。
- Project Module 只保存配置和引用关系，不保存大体积视频帧数据。

---

### 4.2 Video Module：视频信息与预览模块

负责读取视频元数据、处理旋转信息、提供低清预览帧。

建议类：

```text
VideoMetadata
VideoProbeService
VideoFrameReader
PreviewFrameCache
VideoTimebase
```

主要职责：

1. 使用 FFprobe 读取视频分辨率、帧率、时长、编码格式、旋转元数据、是否可变帧率。
2. 根据视频分辨率自动创建透明画布尺寸。
3. 为 UI 预览提供低清帧，不参与最终导出质量判断。
4. 检测可变帧率视频并提示同步风险。
5. 处理手机竖屏视频旋转元数据，确保画布方向与用户看到的视频方向一致。

边界要求：

- Video Module 不读取 GPX/VBO。
- Video Module 不计算圈速。
- Video Module 不决定图层样式。
- 最终导出的透明图层不依赖预览缓存帧。

---

### 4.3 Telemetry Module：遥测数据标准化模块

这是核心数据底座。所有输入格式进入系统后，必须转换为统一 `TelemetryStore`。

建议类：

```text
TelemetryStore
TelemetrySample
TelemetryChannel
TelemetryFrame
TelemetryQualityReport
SourceQuality
TelemetryInterpolator
```

标准遥测字段建议：

| 字段 | 类型 | 说明 |
|---|---|---|
| sample_index | int | 数据点序号 |
| timestamp_ms | float / int | 原始时间戳，毫秒 |
| elapsed_sec | float | 从数据起点开始的相对时间 |
| lat | float / null | 纬度 |
| lon | float / null | 经度 |
| x_m | float / null | 局部平面坐标 x |
| y_m | float / null | 局部平面坐标 y |
| speed_kmh | float / null | 当前速度 |
| heading_deg | float / null | 航向角 |
| accel_long_g | float / null | 纵向 G |
| accel_lat_g | float / null | 横向 G |
| lap_id | int / null | 圈号 |
| lap_time_sec | float / null | 当前圈计时 |
| lap_distance_m | float / null | 当前圈距离 |
| sector_id | int / null | 当前分段编号 |
| sector_time_sec | float / null | 当前分段计时 |
| valid | bool | 数据点是否有效 |
| source_quality | dict | 字段来源与置信度 |

数据类型建议分三层：

| 类型 | 示例 | 说明 |
|---|---|---|
| Raw Channel | lat、lon、raw_speed、raw_heading | 原始文件通道 |
| Derived Channel | x_m、y_m、lap_distance、estimated_g | 计算派生字段 |
| Annotation Channel | valid、source_quality、confidence | 数据质量与标记 |

边界要求：

- 图层组件只能读取 `TelemetryFrame`，不能读取原始 GPX/VBO。
- 计时分析只能依赖 `TelemetryStore` 和 `TrackDefinition`。
- 数据质量标识必须跟随字段传递到渲染层。

---

### 4.4 Parser Module：GPX/VBO 解析模块

负责不同遥测文件格式的解析、字段映射、清洗和标准化前处理。

建议类：

```text
TelemetryParserBase
GpxParser
VboParser
ChannelMapper
TelemetryCleaner
TelemetryNormalizer
QualityReportGenerator
```

处理流程：

```text
GPX/VBO 文件
  ↓
Parser 解析原始字段
  ↓
ChannelMapper 进行字段映射
  ↓
TelemetryCleaner 清洗异常数据
  ↓
TelemetryNormalizer 转成标准 TelemetryStore
  ↓
QualityReportGenerator 生成数据质量报告
```

GPX 处理要求：

1. 读取 `lat`、`lon`、`ele`、`time`。
2. 若速度缺失，通过相邻点距离和时间差估算。
3. 若航向缺失，通过相邻点 bearing 估算。
4. 若 G 力缺失，可以估算，但必须标记为低置信度。
5. 若无时间戳，禁止进入计时分析，只允许做静态轨迹展示或提示用户数据不适合计时。

VBO 处理要求：

1. 采用通道别名表，不写死字段名。
2. 导入后显示通道映射确认窗口。
3. 原始速度、原始 G 力、原始航向优先级高于估算值。
4. 未识别通道保留在 raw channel 中，便于后续调试。

清洗要求：

| 问题 | 处理方式 |
|---|---|
| 空经纬度 | 标记无效或删除 |
| 重复时间戳 | 去重并记录数量 |
| 乱序时间戳 | 排序并记录警告 |
| GPS 跳点 | 标记异常，不默认直接删除 |
| 速度尖峰 | 平滑或标记低置信度 |
| 低采样率 | 生成计时精度提示 |
| G 力估算 | 标记 estimated / low confidence |

---

### 4.5 Geo / Track Module：坐标与赛道定义模块

负责经纬度转本地坐标、显示坐标变换、起终点线、分段线、轨迹视觉校准。

建议类：

```text
ProjectionService
TrackCoordinateSystem
CoordinateTransform
TrackDefinition
TimingLine
SectorLine
DisplayTransform
TrackCalibration
```

必须区分两套坐标：

| 坐标空间 | 用途 | 是否参与计时 |
|---|---|---|
| Calculation Coordinate | 穿线检测、圈速、分段、距离、秒差 | 是 |
| Display Coordinate | UI 显示、卫星底图对齐、小地图绘制 | 否 |

坐标转换接口建议：

```python
class CoordinateTransform:
    def raw_geo_to_calc_xy(self, lat: float, lon: float) -> Point2D:
        pass

    def calc_xy_to_display_xy(self, point: Point2D) -> Point2D:
        pass

    def display_xy_to_calc_xy(self, point: Point2D) -> Point2D:
        pass
```

关键规则：

1. 原始经纬度转换为计算坐标后，用于所有计时和距离计算。
2. 用户拖动轨迹对齐卫星图，只修改 `DisplayTransform`。
3. 用户在卫星图上画起终点线或分段线时，UI 得到的是显示坐标，保存前必须反算为计算坐标。
4. 小地图渲染可以使用显示坐标，但圈速和分段不能使用显示坐标。

---

### 4.6 Timing / Lap Analysis Module：计时分析模块

这是准确性的核心。必须作为纯算法模块实现，不依赖 Qt、OpenCV、FFmpeg。

建议类：

```text
LineCrossingDetector
CrossingInterpolator
LapDetector
SectorDetector
LapTableBuilder
BestLapSelector
DeltaCalculator
TimingPrecisionEstimator
InvalidLapManager
```

输入：

```text
TelemetryStore
TrackDefinition
TimingRules
```

输出：

```text
LapAnalysisResult
SectorAnalysisResult
DeltaProfile
TelemetryStoreWithTimingChannels
```

核心算法要求：

1. 起终点线和分段线均视为带方向的检测线。
2. 相邻两个轨迹点组成车辆运动线段，与检测线相交时判定为一次穿越。
3. 穿越时间不能简单取前一个点或后一个点，必须进行线性插值。
4. 必须支持方向过滤、冷却时间、冷却距离、最低速度阈值。
5. Out lap、in lap、异常圈、用户手动无效圈不参与最快圈比较。
6. 秒差必须按当前圈内距离对齐，不允许按视频全局时间直接对齐。

穿线时间插值：

```text
cross_time = t0 + ratio * (t1 - t0)
```

其中 `ratio` 表示车辆从点 0 到点 1 的运动线段中，与检测线交点所在的比例。

秒差计算流程：

```text
1. 对每圈建立 lap_distance_m → lap_elapsed_sec 插值函数
2. 当前圈在某一帧的圈内距离为 d_current
3. 查询参考圈在同一距离处的耗时 t_ref(d_current)
4. 当前圈耗时为 t_current(d_current)
5. delta = t_current(d_current) - t_ref(d_current)
```

鲁棒性要求：

| 异常 | 处理 |
|---|---|
| 起点线画太长导致多次穿越 | 方向过滤 + 冷却时间 + 冷却距离 |
| GPS 漂移导致反复穿线 | 冷却机制 + 最小有效速度 |
| 低速经过维修区误触发 | 最低速度阈值 |
| 一圈时间明显异常 | 标记异常，允许用户手动排除 |
| 分段线顺序错误 | 支持自动排序和手动排序 |
| 数据采样率低 | 输出理论计时精度提示 |

---

### 4.7 Sync Module：视频与数据同步模块

负责视频时间轴和遥测数据时间轴之间的映射。

建议类：

```text
SyncPoint
SyncModel
SyncOffsetCalculator
DriftChecker
TimebaseConverter
```

核心数据结构：

```python
@dataclass(frozen=True)
class SyncModel:
    video_time_sec: float
    data_elapsed_sec: float
    offset_sec: float

    def video_to_data(self, t_video: float) -> float:
        return t_video - self.offset_sec

    def data_to_video(self, t_data: float) -> float:
        return t_data + self.offset_sec
```

同步公式：

```text
sync_offset = video_time_sec - data_elapsed_sec
t_data = t_video - sync_offset
```

同步方式：

1. 视频帧与轨迹点手动对齐。
2. 用户直接输入偏移量。
3. 多点对齐校验，用于检测视频变速、VFR 可变帧率或数据时间漂移。

边界要求：

- Sync Module 不修改 TelemetryStore 原始时间戳。
- 所有预览和导出都通过 `TimebaseConverter` 获取对应数据时间。
- 若视频时间超出数据范围，图层返回空值，不允许崩溃。

---

### 4.8 Overlay Widget Module：图层组件模块

负责所有透明图层组件的注册、配置、数据绑定和绘制。

内置组件：

| 组件 | 数据依赖 | 说明 |
|---|---|---|
| TrackMapWidget | x_m、y_m、heading、track shape | 赛道路线和当前车位置箭头 |
| SpeedWidget | speed_kmh | 当前车速 |
| GBallWidget | accel_lat_g、accel_long_g | G 力球 |
| TimerWidget | lap_time_sec / global time | 当前计时 |
| DeltaWidget | delta_to_best_sec | 与最快圈秒差 |
| SectorWidget | sector result | 分段计时和刷紫 |
| LapTableWidget | lap table | 前后五圈成绩表 |
| CustomIconWidget | image asset | 自定义 LOGO / 图标 |

基础接口建议：

```python
class OverlayWidgetBase:
    widget_type: str
    schema_version: str

    def config_schema(self) -> dict:
        pass

    def required_channels(self) -> set[str]:
        pass

    def validate_config(self, config: dict) -> ValidationResult:
        pass

    def prepare(self, context: RenderContext) -> None:
        pass

    def render(self, painter, frame: TelemetryFrame, rect: Rect, context: RenderContext) -> None:
        pass

    def serialize(self) -> dict:
        pass

    def deserialize(self, data: dict) -> None:
        pass
```

组件边界：

1. 图层组件只读取 `TelemetryFrame` 和 `RenderContext`。
2. 图层组件不能读取 GPX/VBO 原始文件。
3. 图层组件不能重新计算圈速、分段和秒差。
4. 单个组件渲染失败时，应记录错误并跳过该组件，不影响整帧导出。
5. 组件配置必须可序列化，便于工程保存和模板复用。

---

### 4.9 Canvas Editor Module：透明画布编辑模块

负责图层模块的位置、大小、透明度、层级、锁定、样式配置和布局保存。

建议类：

```text
CanvasScene
WidgetItem
WidgetLayoutManager
SelectionManager
SnapManager
ZIndexManager
StyleEditor
LayoutPresetManager
```

组件公共配置：

```json
{
  "widget_id": "speed_001",
  "widget_type": "speed",
  "visible": true,
  "x": 120,
  "y": 850,
  "width": 300,
  "height": 100,
  "opacity": 1.0,
  "z_index": 3,
  "locked": false,
  "style": {},
  "data_source": "telemetry.default"
}
```

UI 要求：

1. 透明棋盘格背景。
2. 可切换视频预览作为参考背景。
3. 模块可显示/隐藏。
4. 模块可拖动、缩放、锁定。
5. 支持网格吸附和边缘吸附。
6. 支持图层层级调整。
7. 支持样式配置和布局保存。
8. UI 视觉要干净、现代、适合视频创作者使用。

边界要求：

- Canvas Editor 只编辑布局和样式。
- Canvas Editor 不执行最终导出。
- Canvas Editor 不修改遥测数据。
- Canvas Editor 预览效果可以近似，但最终导出必须由 Export Renderer 重新渲染。

---

### 4.10 Render / Preview / Export Module：渲染与导出模块

负责低清预览、单帧透明 RGBA 渲染、PNG 序列导出和 MOV Alpha 视频导出。

建议类：

```text
PreviewRenderer
FrameRenderer
RenderScheduler
RenderContextBuilder
PngSequenceExporter
AlphaVideoExporter
FfmpegCommandBuilder
ExportJob
ExportLogger
TempFileManager
```

渲染分三类：

| 类型 | 目标 | 要求 |
|---|---|---|
| UI 预览 | 快速查看同步和布局 | 可低分辨率、低帧率、跳帧 |
| 单帧渲染 | 某一时刻透明图层 | 必须准确查询数据帧 |
| 最终导出 | 输出透明 PNG/MOV | 必须按目标分辨率和帧率完整渲染 |

最终导出流程：

```text
1. 根据导出帧率生成每一帧 video_time
2. 通过 SyncModel 将 video_time 转为 data_time
3. 通过 TelemetryInterpolator 获取 TelemetryFrame
4. 创建透明 RGBA 画布
5. 按 z_index 顺序调用 OverlayWidgetEngine 渲染组件
6. 写入 PNG 序列或传入 FFmpeg 管道
7. 使用 FFmpeg 编码为带 Alpha 的 MOV / WebM
8. 写入 export_manifest.json
9. 清理临时文件或按用户设置保留
```

导出 Manifest 建议：

```json
{
  "project_name": "kart_overlay_project",
  "video_file": "input.mp4",
  "data_file": "input.gpx",
  "overlay_start_video_time_sec": 34.520,
  "data_start_time": "2026-05-28T14:23:11.240Z",
  "data_duration_sec": 312.850,
  "canvas_width": 1920,
  "canvas_height": 1080,
  "fps": 60,
  "export_format": "mov_prores_4444",
  "alpha": true,
  "sync_offset_sec": 34.520,
  "quality_summary": {}
}
```

导出可靠性要求：

1. 导出前进行完整预检查：视频元数据、遥测数据、同步关系、图层配置、输出路径、磁盘空间。
2. 导出中支持进度、取消和日志。
3. 导出失败必须保留日志和错误原因。
4. 中途取消必须清理临时文件。
5. MOV Alpha 失败时应允许用户改用 PNG 序列兜底。
6. 未勾选的组件不得出现在导出结果中。
7. 导出画布尺寸默认与原视频一致。
8. 导出的透明背景必须可被 Premiere、DaVinci Resolve 等剪辑软件识别。

---

### 4.11 Infrastructure Module：基础设施适配模块

负责外部依赖和系统资源适配。

建议类：

```text
FfprobeAdapter
FfmpegAdapter
FileSystemAdapter
LogService
TileCacheService
ResourceManager
PackagePathResolver
```

职责：

1. 调用 FFprobe 获取视频信息。
2. 调用 FFmpeg 执行编码。
3. 管理临时文件、缓存、日志文件和导出目录。
4. 管理地图瓦片或用户自备底图。
5. 管理打包后的资源路径。

边界要求：

- Infrastructure 不写业务规则。
- FFmpeg 命令必须通过参数列表执行，禁止拼接 shell 字符串。
- 所有外部进程调用都要有超时、错误码检查、stderr 日志保存。

安全示例：

```python
subprocess.run(
    ["ffmpeg", "-y", "-framerate", str(fps), "-i", input_pattern, output_path],
    shell=False,
    timeout=timeout_sec,
    check=True
)
```

禁止：

```python
os.system(f"ffmpeg -i {user_input} {output_path}")
```

---

## 5. 关键数据流设计

### 5.1 导入视频数据流

```text
用户导入视频
  ↓
VideoProbeService 调用 FFprobe
  ↓
读取分辨率、帧率、时长、编码、旋转元数据、VFR 信息
  ↓
ProjectState 更新视频元数据
  ↓
Canvas 初始化透明画布尺寸
  ↓
UI 展示视频信息和同步风险提示
```

异常处理：

| 异常 | 处理 |
|---|---|
| 视频无法读取 | 显示明确错误，记录 FFprobe 日志 |
| 旋转元数据存在 | 自动修正画布方向 |
| 可变帧率 | 提示同步风险，建议转恒定帧率 |
| 视频无音频 | 不影响功能，仅记录信息 |
| 帧率为 59.94 | 保留精确帧率，不强制变成 60 |

---

### 5.2 导入遥测数据流

```text
用户导入 GPX/VBO
  ↓
Parser 解析原始数据
  ↓
ChannelMapper 完成字段映射
  ↓
TelemetryCleaner 清洗异常点
  ↓
ProjectionService 经纬度转局部坐标
  ↓
TelemetryNormalizer 生成 TelemetryStore
  ↓
QualityReportGenerator 生成数据质量报告
  ↓
UI 展示数据质量、采样率、缺失字段和估算字段
```

异常处理：

| 异常 | 处理 |
|---|---|
| GPX 无时间戳 | 禁止计时分析，只允许轨迹展示或提示不可用 |
| GPX 无速度 | 根据距离/时间估算，并标记 estimated |
| GPX 无 G 力 | 估算或隐藏 G 力模块，并标记低置信度 |
| VBO 通道名异常 | 弹出通道映射窗口 |
| GPS 跳点 | 标记异常点，不默认删除 |
| 数据采样率低 | 显示计时精度风险 |

---

### 5.3 赛道线编辑与计时数据流

```text
用户在地图上设置起终点线 / 分段线
  ↓
MapEditor 得到显示坐标
  ↓
CoordinateTransform 反算为计算坐标
  ↓
TrackDefinition 更新
  ↓
LapDetector 执行穿线检测
  ↓
SectorDetector 执行分段检测
  ↓
BestLapSelector 识别参考圈
  ↓
DeltaCalculator 生成秒差曲线
  ↓
TelemetryStore 写入 lap/sector/delta 派生通道
  ↓
UI 更新圈速表、分段表和预览
```

关键要求：

- 画线坐标必须反算为计算坐标后再保存。
- 修改起点线、分段线、方向、冷却时间、最低速度阈值后，必须重新计算圈速和秒差。
- 计时结果必须可重复：同一份数据、同一套设置，每次计算结果应一致。

---

### 5.4 同步与预览数据流

```text
用户选择视频帧和对应轨迹点
  ↓
SyncOffsetCalculator 计算 offset
  ↓
SyncModel 保存同步关系
  ↓
用户拖动视频时间轴
  ↓
TimebaseConverter: video_time → data_time
  ↓
TelemetryInterpolator 查询 TelemetryFrame
  ↓
OverlayWidgetEngine 渲染预览图层
  ↓
UI 展示视频 + 透明图层叠加预览
```

关键要求：

- 预览只用于交互确认，不作为最终导出结果。
- 当前视频时间超出数据范围时，各图层显示 `--`、灰色或隐藏，不允许崩溃。
- 多点同步校验发现时间漂移时，应提示用户可能存在 VFR 或数据时间漂移。

---

### 5.5 透明贴片导出数据流

```text
用户点击导出
  ↓
ExportPreflightChecker 检查工程完整性
  ↓
ExportJob 创建导出任务
  ↓
逐帧生成 video_time
  ↓
video_time → data_time
  ↓
TelemetryInterpolator 获取 TelemetryFrame
  ↓
FrameRenderer 创建透明 RGBA 画布
  ↓
OverlayWidgetEngine 按 z_index 渲染所有可见组件
  ↓
写入 PNG 或传入 FFmpeg
  ↓
生成 MOV / PNG 序列
  ↓
写入 export_manifest.json
  ↓
显示导出完成信息和剪辑软件放置时间点
```

导出前检查项：

| 检查项 | 要求 |
|---|---|
| 视频元数据 | 分辨率、帧率、时长已读取 |
| 遥测数据 | TelemetryStore 有效 |
| 同步关系 | 至少存在一个有效 offset |
| 计时结果 | 起终点线有效，圈速表合理 |
| 图层配置 | 至少有一个可见图层 |
| 输出路径 | 可写、空间足够 |
| Alpha 格式 | MOV ProRes 4444 或 PNG 序列可用 |
| FFmpeg | 可执行文件存在且可调用 |

---

## 6. 数据传输鲁棒性设计

### 6.1 统一 Frame 查询接口

所有图层组件在某个时间点获取数据时，不允许直接查 DataFrame，不允许自己插值。必须通过统一接口：

```python
class TelemetryFrameProvider:
    def get_frame_by_data_time(self, data_time_sec: float) -> TelemetryFrame:
        pass

    def get_frame_by_video_time(self, video_time_sec: float, sync: SyncModel) -> TelemetryFrame:
        pass
```

`TelemetryFrame` 应为只读对象：

```python
@dataclass(frozen=True)
class TelemetryFrame:
    data_elapsed_sec: float
    video_time_sec: float | None
    lat: float | None
    lon: float | None
    x_m: float | None
    y_m: float | None
    speed_kmh: float | None
    heading_deg: float | None
    accel_lat_g: float | None
    accel_long_g: float | None
    lap_id: int | None
    sector_id: int | None
    lap_time_sec: float | None
    sector_time_sec: float | None
    delta_to_best_sec: float | None
    quality: dict
```

### 6.2 缺失值与异常值策略

| 情况 | 系统处理 | UI/导出表现 |
|---|---|---|
| 速度缺失 | 估算速度 | 可显示 Estimated 标识 |
| G 力缺失 | 估算或返回 null | G 力球隐藏、灰显或显示低置信度 |
| heading 缺失 | 通过相邻点估算 | 低速时使用上一有效 heading |
| 当前时间超出数据范围 | 返回 empty frame | 显示 `--` 或隐藏模块 |
| 单个组件渲染失败 | 捕获异常并记录日志 | 该组件跳过，其他组件继续 |
| 数据跳点 | 标记异常 | 可参与平滑但标记低置信度 |
| 采样率低 | 正常计算但标记风险 | 数据质量报告提示计时精度有限 |

### 6.3 插值策略

| 字段 | 插值方式 |
|---|---|
| x_m / y_m | 线性插值 |
| speed_kmh | 线性插值 + 平滑 |
| heading_deg | 角度插值，处理 0/360 跳变 |
| accel_lat_g / accel_long_g | 线性插值 + 平滑 |
| lap_time_sec | 按时间直接计算 |
| lap_id / sector_id | 阶跃值，不线性插值 |
| delta_to_best_sec | 距离对齐后插值，可做显示平滑 |

### 6.4 日志与可追溯性

必须记录以下日志：

1. 视频导入日志。
2. 遥测导入和字段映射日志。
3. 数据清洗日志。
4. 圈速/分段计算日志。
5. 同步点和 offset 日志。
6. 导出参数日志。
7. FFmpeg stderr 日志。
8. 组件渲染异常日志。

导出文件夹建议包含：

```text
output/
  overlay.mov
  export_manifest.json
  export.log
  quality_report.json
```

---

## 7. UI 架构设计要求

UI 要做漂亮，但不能把业务逻辑写进 UI 控件。推荐采用接近 MVVM 的组织方式：

```text
Qt Widget / QGraphicsScene
  ↓
ViewModel
  ↓
Application Service
  ↓
Domain Core
```

### 7.1 主界面布局

建议主界面保持专业剪辑软件风格：

```text
┌──────────────────────────────────────────────┐
│ 顶部菜单：文件 / 数据 / 地图 / 图层 / 导出 / 帮助 │
├───────────────┬──────────────────────┬───────┤
│ 左侧工程树     │ 中央预览 / 地图 / 画布 │ 右侧属性 │
│ 素材列表       │ 视频预览 / 地图编辑    │ 参数面板 │
│ 模块列表       │ 透明画布编辑          │ 样式配置 │
├───────────────┴──────────────────────┴───────┤
│ 底部时间轴：视频时间 / 数据时间 / 圈号 / 分段      │
└──────────────────────────────────────────────┘
```

### 7.2 UI 子模块

| UI 模块 | 职责 |
|---|---|
| MainWindow | 主窗口、菜单、整体布局 |
| ProjectPanel | 工程树、素材列表、数据状态 |
| MapEditor | 轨迹显示、底图、起终点线、分段线、视觉校准 |
| CanvasEditor | 透明画布、组件拖动、缩放、锁定、层级 |
| SyncPanel | 视频帧和轨迹点同步 |
| ExportDialog | 导出格式、帧率、范围、进度、日志 |
| PropertyPanel | 当前选中模块的属性和样式配置 |

### 7.3 UI 美观与工程实现约束

1. 使用统一主题系统管理颜色、字体、边框、阴影、圆角。
2. 使用 QGraphicsView/QGraphicsScene 实现地图和画布交互。
3. 所有图层组件在 UI 画布中应有选中框、缩放点、锁定状态、层级提示。
4. 属性面板应基于组件 `config_schema()` 自动生成，避免每个组件单独写一套配置 UI。
5. UI 操作必须通过命令模式或 Application Service 修改工程状态，避免控件直接改底层数据。
6. 长任务如导入、分析、导出必须放到后台任务线程，UI 不得卡死。
7. 后台任务不能直接修改 Qt 控件，必须通过信号或事件返回主线程。

---

## 8. 安全边界设计

### 8.1 文件输入边界

用户导入的工程文件、GPX、VBO、图片和视频都必须视为不可信输入。

必须实现：

1. 文件大小限制。
2. 文件扩展名和内容基本校验。
3. JSON 工程文件 schema 校验。
4. 路径规范化，禁止路径穿越。
5. 素材路径失效时提示重新定位，不自动删除。
6. 读取失败时捕获异常并给出明确错误。

### 8.2 FFmpeg 外部进程边界

FFmpeg 调用必须满足：

1. `shell=False`。
2. 命令参数使用 list，不拼接 shell 字符串。
3. 保存 stderr 到日志。
4. 检查返回码。
5. 设置超时和取消机制。
6. 导出失败时不删除日志。

### 8.3 图层组件边界

1. 图层组件只能读取输入数据，不能修改遥测数据。
2. 图层组件异常不能导致整帧失败。
3. 组件配置必须通过 schema 校验。
4. 首期不开放任意外部 Python 插件执行。
5. 自定义图标模块只允许读取用户指定的图片资源，不允许执行脚本。

### 8.4 地图与底图边界

1. 卫星底图只用于视觉参考和编辑辅助。
2. 视觉校准不允许污染计算坐标。
3. 在线瓦片服务需要考虑授权和调用限制。
4. 地图瓦片加载失败时，应回退到无底图轨迹显示。

### 8.5 工程保存边界

工程保存必须采用：

```text
写入 project.tmp
  ↓
校验 project.tmp 可解析
  ↓
备份旧 project 文件
  ↓
原子替换
```

防止崩溃、断电或写入异常导致工程文件损坏。

---

## 9. 推荐代码目录结构

```text
kart_overlay/
  app.py

  ui/
    main_window.py
    project_panel.py
    map_editor.py
    canvas_editor.py
    sync_panel.py
    export_dialog.py
    property_panel.py
    viewmodels/
      project_viewmodel.py
      map_viewmodel.py
      canvas_viewmodel.py
      export_viewmodel.py

  application/
    import_video_usecase.py
    import_telemetry_usecase.py
    edit_track_usecase.py
    recalculate_timing_usecase.py
    sync_video_usecase.py
    export_overlay_usecase.py
    project_service.py
    task_runner.py

  domain/
    telemetry/
      models.py
      store.py
      frame_provider.py
      interpolator.py
      quality.py
    track/
      models.py
      coordinate_system.py
      transform.py
      projection.py
    timing/
      line_crossing.py
      lap_detector.py
      sector_detector.py
      delta_calculator.py
      rules.py
      precision.py
    overlay/
      widget_base.py
      registry.py
      render_context.py
      layout.py

  infrastructure/
    parsers/
      parser_base.py
      gpx_parser.py
      vbo_parser.py
      channel_mapper.py
      telemetry_cleaner.py
      telemetry_normalizer.py
      quality_report_generator.py
    video/
      ffprobe.py
      frame_reader.py
      preview_cache.py
    render/
      qt_painter_adapter.py
      frame_renderer.py
      preview_renderer.py
      ffmpeg_exporter.py
      png_sequence_exporter.py
      export_logger.py
      temp_file_manager.py
    persistence/
      project_repository.py
      schema_validator.py
      atomic_saver.py
    map/
      tile_cache.py
      image_basemap.py
    logging/
      log_service.py

  widgets/
    track_map_widget.py
    speed_widget.py
    g_ball_widget.py
    timer_widget.py
    delta_widget.py
    sector_widget.py
    lap_table_widget.py
    custom_icon_widget.py

  resources/
    themes/
    icons/
    default_layouts/

  tests/
    unit/
      test_line_crossing.py
      test_lap_detector.py
      test_sector_detector.py
      test_delta_calculator.py
      test_interpolator.py
      test_coordinate_transform.py
    integration/
      test_gpx_import_pipeline.py
      test_vbo_import_pipeline.py
      test_sync_pipeline.py
      test_export_png_sequence.py
      test_export_manifest.py
```

---

## 10. 必须优先保证的可靠性指标

| 能力 | 可靠性要求 |
|---|---|
| 计时 | 穿线检测可重复；插值计算稳定；异常圈可识别和排除 |
| 分段 | 分段线顺序稳定；刷紫规则明确；缺失数据不崩溃 |
| 秒差 | 按圈内距离对齐，不按全局视频时间粗暴比较 |
| 同步 | video_time 和 data_time 转换统一经过 SyncModel |
| 贴片 | 图层组件按 z_index 稳定渲染；单组件失败不影响整帧 |
| 导出 | 最终导出重新全质量渲染，不复用低清预览 |
| Alpha | MOV ProRes 4444 和 PNG 序列必须至少有一种稳定可用 |
| 工程保存 | 原子写入，避免工程损坏 |
| 日志 | 导入、分析、同步、导出都有可追踪日志 |

---

## 11. 开发红线

以下做法禁止出现：

1. 把 GPX/VBO 解析逻辑写进 Qt 窗口类。
2. 把圈速、分段、秒差计算写进 UI 控件事件里。
3. 图层组件自己读取原始数据文件。
4. 图层组件自己重新计算圈速或秒差。
5. 地图视觉校准直接修改原始 GPS 坐标。
6. 预览帧直接作为最终导出帧。
7. 使用字符串拼接执行 FFmpeg 命令。
8. 工程文件直接覆盖写入，不做临时文件和原子替换。
9. 数据字段缺失时直接崩溃。
10. 导出失败不保存日志。

---

## 12. 最终架构判断

本软件的核心不是“做一个好看的 Qt 工具”，而是建立一条可靠的数据生产链：

```text
原始 GPX/VBO 数据
  ↓
标准遥测数据
  ↓
稳定计时分析
  ↓
可同步的视频时间轴
  ↓
可配置图层组件
  ↓
可验证透明贴片导出
```

UI 必须漂亮、顺手、适合内容创作者使用，但 UI 不能成为业务逻辑中心。核心计时、分段、秒差、同步、贴片和导出必须沉到 Domain Core、Overlay Engine 和 Export Engine 中，形成稳定、可测试、可回归的底层能力。

开发时应先按上述架构搭好模块边界和接口，再实现具体功能。只要边界正确，后续调整 UI、增加图层样式、优化地图体验、增加新数据通道，都不会破坏核心计时和导出链路。
