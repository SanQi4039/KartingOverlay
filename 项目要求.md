# 车载视频透明数据图层生成软件需求文档（PRD + 项目实施方案）

版本：v1.0  
日期：2026-06-08  
定位：桌面端 Python/Qt 软件，用于将 GPX/VBO 轨迹数据解析为可叠加到车载视频上的透明背景动态数据图层  
输出对象：产品经理、项目经理、软件开发、后期剪辑使用者

---

## 0. 一句话结论

本软件的核心目标不是“把数据直接压到原视频里”，而是构建一个桌面端遥测图层生成工具：用户导入车载视频与 GPX/VBO 数据后，可以在地图上自定义起终点线、分段线、数据对齐点、模块位置与样式，最终导出一个带 Alpha 通道的透明视频图层或 PNG 序列，用于 Premiere、DaVinci Resolve、剪映专业版等剪辑软件二次叠加。

从工程角度看，本项目应拆成四条主线：

1. **数据主线**：GPX/VBO 解析、轨迹清洗、速度/航向/G 力/圈速/分段/秒差计算。
2. **地图主线**：轨迹显示、卫星底图、起终点/分段线编辑、轨迹与卫星地图手动对齐。
3. **画布主线**：透明图层画布、模块勾选、拖拽、缩放、样式配置、布局保存。
4. **导出主线**：视频同步预览、低清预览、透明图层渲染、FFmpeg 编码、工程文件复用。

---

## 1. 项目背景与目标

### 1.1 背景

现有车载视频通常只能记录视觉画面，难以直观展示车辆在赛道上的速度变化、线路位置、G 力、圈速变化、分段表现和与最快圈的差距。对于卡丁车/赛道复盘类视频而言，仅靠口播或静态截图解释效率较低；如果能在视频中增加专业的数据图层，可以显著提升内容的专业度、信息密度和观众理解效率。

市面上的专业遥测视频软件通常存在以下问题：

- 对数据格式、设备品牌、授权体系依赖较强；
- 自定义起点、分段和图层布局的自由度有限；
- 对中文用户和短视频剪辑流程不够友好；
- 输出结果往往直接合成原视频，不便于后期二次调整；
- 对 GPX、VBO、不同帧率视频之间的手动同步能力不够直观。

因此，本项目的目标是开发一个面向个人赛车/卡丁车视频复盘的桌面工具，重点解决“数据解析—同步—可视化—透明图层导出”的完整流程。

### 1.2 产品目标

软件需要实现以下目标：

1. 支持导入已有车载视频，并自动读取视频画布尺寸、帧率、时长等基础信息。
2. 支持导入 GPX 和 VBO 文件，并解析轨迹、时间、速度、航向、加速度/G 力等数据。
3. 支持在赛道地图上自定义计时起点/终点线和分段线。
4. 支持在卫星地图背景上编辑轨迹，并允许用户手动拖动轨迹图层进行视觉对齐。
5. 支持将赛道路线图、当前车位置、速度、G 力球、当前计时、最快圈秒差、分段计时、前后圈成绩表等模块输出为透明图层。
6. 支持模块级勾选、拖动、缩放、样式配置和布局保存。
7. 支持通过视频帧与 GPS 数据点进行手动同步，并在导出时保留“视频起始位置”和“数据起始位置”的对应关系。
8. 支持导出透明背景视频或透明 PNG 序列，便于后期剪辑软件二次调整。
9. 软件应以 Python 桌面端为主，优先考虑 Qt5/PyQt5 或兼容 Qt Widgets 体系，不采用网页应用架构。
10. 软件最终应可打包为 Windows `.exe`，尽可能降低用户部署门槛。

---

## 2. 用户角色与使用场景

### 2.1 用户角色

| 角色 | 需求重点 | 典型操作 |
|---|---|---|
| 内容创作者 | 快速生成专业数据叠加图层 | 导入视频和 GPX，设置起点/分段，导出透明图层 |
| 赛道复盘者 | 分析圈速、线路、分段和速度损失 | 查看最快圈、当前圈、分段差距、G 力变化 |
| 开发/维护者 | 扩展新图表、新数据格式、新导出格式 | 新增图层模块、新增解析器、新增渲染样式 |
| 后期剪辑者 | 保持视频可编辑性 | 只导出透明图层，在剪辑软件中调整位置/透明度/裁切 |

### 2.2 核心使用场景

#### 场景 A：制作卡丁车最快圈复盘视频

用户导入一段完整车载视频和对应 GPX 文件。软件解析轨迹后，用户在卫星地图上点击设置起终点线和若干分段线。软件自动计算每圈圈速、分段计时和最快圈。用户将赛道小地图、速度、G 力球、当前圈计时、与最快圈秒差、分段成绩表拖到合适位置，最终导出透明图层，放入剪辑软件叠加到原视频上。

#### 场景 B：比较某一圈与最快圈的差距

用户选择“当前圈对比最快圈”模式。软件在当前播放时间显示当前圈相对最快圈同距离位置的时间差，例如 `+0.324s` 或 `-0.112s`。当当前圈在某个分段刷新最佳分段时，分段模块显示紫色提示。

#### 场景 C：视频与 GPS 时间不同步

用户导入视频后，发现视频开头和 GPS 数据开头不一致。用户在视频预览中定位到车辆经过某个明显位置的画面，再在轨迹数据中选择对应位置，软件计算视频时间轴与数据时间轴之间的偏移量。用户通过低清预览确认同步结果，再进行最终导出。

#### 场景 D：卫星地图和 GPS 轨迹视觉不重合

由于 GPS 漂移或卫星图定位偏差，轨迹线与实际赛道视觉位置不完全重合。用户可以在地图编辑界面拖动轨迹图层，必要时进行旋转和缩放校准。该校准主要用于视觉对齐，不应破坏原始 GPS 计算数据。

---

## 3. 产品范围

### 3.1 本期必须实现范围

本期必须实现的是“可用的透明遥测图层生成工具”，而不是完整的专业遥测分析平台。核心范围如下：

1. 导入视频；
2. 导入 GPX；
3. 导入 VBO；
4. 轨迹显示；
5. 起终点线编辑；
6. 分段线编辑；
7. 圈速计算；
8. 分段计时计算；
9. 最快圈识别；
10. 与最快圈秒差计算；
11. 视频与数据手动同步；
12. 透明画布编辑；
13. 基础数据模块渲染；
14. 透明视频/PNG 序列导出；
15. 工程文件保存和再次打开。

### 3.2 本期不优先实现范围

以下内容不应进入首个可用版本的核心开发范围：

1. 自动识别赛道弯角编号；
2. 自动生成口播分析文案；
3. 多车同屏比较；
4. 实时直播叠加；
5. 云端账号系统；
6. 自动剪辑视频；
7. 与剪辑软件插件级联动；
8. 高精度 IMU 标定；
9. 车辆动力学深度建模；
10. 商业地图服务的深度集成。

### 3.3 关键边界说明

1. 软件只负责生成透明数据图层，不默认把图层压制到原视频。
2. 透明图层导出时的画布尺寸默认等于原视频尺寸。
3. 卫星地图主要用于设置起点、分段和轨迹校准；最终输出图层默认不包含卫星底图，除非用户显式开启“小地图背景”。
4. GPX 文件通常只包含位置和时间，不一定包含真实速度、航向和 G 力。缺失数据需要估算，并在 UI 中标记为“估算值”。
5. VBO 文件可能包含更完整的通道，例如速度、横向 G、纵向 G、航向、距离等，应优先使用 VBO 原始通道。
6. 视频同步采用“用户手动选点 + 软件计算偏移”的方式，不承诺自动音画/GPS 对齐。

---

## 4. 输入与输出

### 4.1 输入文件

#### 4.1.1 视频输入

软件应支持常见视频容器和编码格式，实际兼容性由 FFmpeg 解码能力决定。目标支持：

- `.mp4`
- `.mov`
- `.mkv`
- `.avi`
- `.m4v`
- `.webm`

需要读取的视频信息：

- 分辨率；
- 帧率；
- 总时长；
- 编码格式；
- 是否为可变帧率；
- 音频轨存在情况；
- 起始时间戳；
- 旋转元数据，例如手机竖屏视频可能带旋转信息。

#### 4.1.2 GPX 输入

GPX 解析字段：

| 字段 | 必需 | 来源 | 说明 |
|---|---:|---|---|
| latitude | 是 | `trkpt lat` | 纬度 |
| longitude | 是 | `trkpt lon` | 经度 |
| elevation | 否 | `ele` | 海拔 |
| timestamp | 强烈建议 | `time` | 时间戳，圈速计算依赖该字段 |
| speed | 否 | extension 或计算 | 可由相邻点距离/时间差估算 |
| course/heading | 否 | extension 或计算 | 可由相邻点方位角估算 |
| acceleration/G | 否 | extension 或计算 | GPX 通常不直接提供，估算精度有限 |

#### 4.1.3 VBO 输入

VBO 解析字段应采用“通道自适应”策略。不同 VBOX 设备或导出设置可能导致通道名称不同，因此不能写死单一字段名。需要建立通道映射表。

常见通道类型：

| 类型 | 可能通道名 | 用途 |
|---|---|---|
| 时间 | time、utc、sats time | 数据时间轴 |
| 纬度 | lat、latitude | 轨迹计算 |
| 经度 | long、longitude | 轨迹计算 |
| 速度 | velocity、speed、kmh | 速度显示、距离积分 |
| 航向 | heading、course | 车头箭头方向 |
| 横向 G | latacc、lateral g | G 力球横轴 |
| 纵向 G | longacc、longitudinal g | G 力球纵轴 |
| 距离 | distance | 圈内距离、秒差插值 |
| 高度 | height、altitude | 可选 |

VBO 导入时需要显示“通道映射确认窗口”，允许用户手动修正字段对应关系。

### 4.2 输出文件

#### 4.2.1 首选输出

| 输出格式 | 用途 | 优点 | 风险 |
|---|---|---|---|
| `.mov` + ProRes 4444 | 专业剪辑软件叠加 | 兼容性较好，支持 Alpha | 文件较大 |
| PNG 序列 | 最高兼容备用方案 | 每帧透明，几乎所有软件可导入 | 文件数量多，占空间大 |

#### 4.2.2 可选输出

| 输出格式 | 用途 | 优点 | 风险 |
|---|---|---|---|
| `.webm` + VP9 Alpha | 网络/部分软件 | 文件较小 | 剪辑软件兼容性不稳定 |
| `.mov` + Animation | 老旧兼容方案 | 支持 Alpha | 编码老、文件大 |

#### 4.2.3 导出附加文件

每次导出应同时生成一个 JSON 记录文件：

```json
{
  "project_name": "kart_overlay_project",
  "video_file": "input.mp4",
  "data_file": "input.gpx",
  "overlay_start_video_time_sec": 34.520,
  "data_start_time": "2026-05-28T14:23:11.240Z",
  "data_duration_sec": 312.850,
  "canvas_width": 1920,
  "canvas_height": 1080,
  "fps": 60,
  "export_format": "mov_prores_4444",
  "alpha": true
}
```

该 JSON 的作用是让用户在剪辑软件中明确知道：透明图层应该从原视频的哪一个时间点开始放置。

---

## 5. 核心功能需求

## 5.1 工程管理

### 功能描述

软件需要以“工程”为单位管理所有素材、数据、配置和图层布局。

### 功能要求

1. 新建工程；
2. 打开工程；
3. 保存工程；
4. 另存为工程；
5. 记住最近打开文件；
6. 工程内记录视频路径、数据路径、同步偏移、起终点线、分段线、图层布局、样式主题和导出设置；
7. 若素材路径失效，打开工程时提示重新定位文件。

### 工程文件建议

工程文件后缀建议为：`.kotproj` 或 `.kartoverlay`。内部可以使用 JSON。

示例结构：

```json
{
  "version": "1.0",
  "video": {},
  "telemetry": {},
  "track": {},
  "sync": {},
  "widgets": [],
  "export": {}
}
```

---

## 5.2 视频导入与画布识别

### 功能描述

用户导入已有车载视频，软件自动获取视频尺寸，并以此创建透明图层画布。

### 功能要求

1. 支持拖拽导入视频；
2. 支持文件选择导入视频；
3. 自动读取视频宽度、高度、帧率、时长；
4. 自动处理视频旋转元数据；
5. 自动创建同尺寸透明画布；
6. 允许用户手动覆盖画布尺寸；
7. 允许用户选择导出帧率：
   - 跟随视频帧率；
   - 30 fps；
   - 60 fps；
   - 自定义 fps。

### 验收标准

1. 导入 1920×1080 视频后，默认透明画布为 1920×1080。
2. 导入竖屏视频后，画布方向与播放器看到的方向一致。
3. 视频帧率为 59.94 fps 时，导出帧率默认应保持 59.94 或提示用户转换为 60 fps。

---

## 5.3 GPX/VBO 数据导入与标准化

### 功能描述

将不同格式的数据文件统一转换为内部标准遥测数据表。

### 内部标准数据结构

| 字段 | 类型 | 说明 |
|---|---|---|
| `sample_index` | int | 数据点序号 |
| `timestamp_ms` | int/float | 数据时间戳，毫秒 |
| `elapsed_sec` | float | 从数据起点开始的相对时间 |
| `lat` | float | 纬度 |
| `lon` | float | 经度 |
| `x_m` | float | 局部平面坐标 x |
| `y_m` | float | 局部平面坐标 y |
| `speed_kmh` | float | 速度 |
| `heading_deg` | float | 航向角 |
| `accel_long_g` | float | 纵向 G |
| `accel_lat_g` | float | 横向 G |
| `lap_id` | int | 圈号 |
| `lap_time_sec` | float | 当前圈累计时间 |
| `lap_distance_m` | float | 当前圈累计距离 |
| `sector_id` | int | 当前分段编号 |
| `sector_time_sec` | float | 当前分段累计时间 |
| `valid` | bool | 数据点是否有效 |
| `source_quality` | str | 原始值/估算值/缺失 |

### 数据清洗要求

1. 删除空经纬度点；
2. 删除重复时间戳点；
3. 对乱序时间戳排序；
4. 对异常跳点进行标记，而不是默认删除；
5. 对缺失速度进行估算；
6. 对缺失航向进行估算；
7. 对缺失 G 力进行估算并标记为低置信度；
8. 导入结束后显示数据质量报告。

### 数据质量报告内容

| 指标 | 示例 |
|---|---|
| 数据格式 | GPX / VBO |
| 数据点数量 | 18642 |
| 采样率估计 | 10 Hz |
| 有效经纬度比例 | 99.7% |
| 时间戳缺失比例 | 0% |
| 速度来源 | 原始/估算 |
| G 力来源 | 原始/估算/缺失 |
| GPS 跳点数量 | 3 |
| 可计算圈速 | 是/否 |

---

## 5.4 地图与轨迹编辑

### 功能描述

地图界面用于显示赛道轨迹、卫星底图、起终点线、分段线，以及用于数据同步的参考点。

### 功能要求

1. 显示轨迹线；
2. 显示当前播放位置；
3. 支持缩放、拖拽地图；
4. 支持显示/隐藏卫星底图；
5. 支持轨迹图层相对卫星图平移；
6. 支持轨迹图层相对卫星图旋转；
7. 支持轨迹图层相对卫星图缩放；
8. 支持重置轨迹图层校准；
9. 支持点击地图创建起终点线；
10. 支持点击地图创建分段线；
11. 支持拖动线端点调整位置；
12. 支持设置线的通过方向；
13. 支持删除和重命名分段线。

### 关键设计原则

地图编辑中需要区分两个坐标空间：

1. **计算坐标空间**：用于圈速、分段、距离、速度等计算，应使用原始 GPS/VBO 轨迹转换后的局部平面坐标。
2. **显示坐标空间**：用于卫星地图视觉对齐，可以加入平移、旋转、缩放变换。

用户拖动轨迹对齐卫星图时，默认只改变显示坐标空间，不直接修改原始数据。若用户在校准后的卫星地图上画分段线，软件需要将显示坐标反算回计算坐标，避免分段计算与显示不一致。

---

## 5.5 自定义计时起点/终点线

### 功能描述

用户可以在赛道轨迹地图上自行选择计时起点。计时起点本质上是一条带方向的检测线。

### 功能要求

1. 用户点击“添加起终点线”；
2. 在地图上点击两个点生成一条线段；
3. 软件显示线段名称，例如 `Start/Finish`；
4. 用户可以拖动两个端点调整线段；
5. 用户可以设置通过方向；
6. 软件根据轨迹点是否穿越该线段判断圈开始/圈结束；
7. 支持设置最小有效圈时间；
8. 支持设置重复触发冷却时间；
9. 支持设置重复触发冷却距离；
10. 支持手动排除某些异常圈。

### 圈速计算逻辑

当相邻两个 GPS 点之间的轨迹线段与起终点线相交时，判定为一次通过。相交时刻不应简单取前一个点或后一个点的时间，而应进行线性插值：

```text
cross_time = t0 + ratio * (t1 - t0)
```

其中 `ratio` 表示从点 0 到点 1 的线段上，与起终点线交点所在的比例位置。

### 防误触发规则

| 问题 | 处理方式 |
|---|---|
| 起点线画得太长，车辆在附近多次穿过 | 加入方向过滤 |
| GPS 漂移导致反复穿线 | 加入冷却时间/冷却距离 |
| 车辆在维修区低速移动误触发 | 设置最小有效速度 |
| 一圈时间明显异常 | 允许用户手动标记无效圈 |

---

## 5.6 自定义分段线

### 功能描述

用户可以在地图上自行设置多个分段线，用于计算 T1、T2、T3 或自定义分段成绩。

### 功能要求

1. 支持添加任意数量分段线；
2. 分段线支持命名，例如 `S1`、`T1-T2`、`Hairpin Exit`；
3. 分段线支持拖动、旋转、删除；
4. 分段顺序可自动按赛道行驶方向排序，也可手动排序；
5. 每圈经过分段线时记录分段用时；
6. 分段成绩表支持显示：
   - 当前分段时间；
   - 当前圈分段时间；
   - 本场最佳分段；
   - 当前圈相对最佳分段差值；
   - 是否刷新最佳分段。

### 刷紫规则建议

| 颜色 | 含义 |
|---|---|
| 紫色 | 本场已加载数据中的最快分段 |
| 绿色 | 当前圈个人改善，但不是全场最快分段 |
| 黄色/白色 | 正常分段，无改善 |
| 红色 | 当前分段明显慢于参考值 |
| 灰色 | 无效/未完成/数据不足 |

颜色规则应允许用户在设置中自定义。

---

## 5.7 圈速、分段与最快圈秒差

### 功能描述

软件需要基于起终点线和分段线计算完整圈速、分段计时，以及当前圈相对最快圈的秒差。

### 圈速表字段

| 字段 | 说明 |
|---|---|
| 圈号 | Lap 1、Lap 2 等 |
| 起始时间 | 数据时间轴中的圈开始时刻 |
| 结束时间 | 数据时间轴中的圈结束时刻 |
| 圈速 | 完整圈用时 |
| 是否有效 | 是否参与最快圈比较 |
| 分段成绩 | S1/S2/S3/... |
| 与最快圈差值 | `+0.532s` |

### 最快圈判定规则

1. 默认选取所有有效完整圈中的最短圈；
2. out lap 和 in lap 默认不参与最快圈；
3. 用户可以手动设置某圈为无效；
4. 用户可以指定参考圈，不一定必须是最快圈。

### 与最快圈秒差计算逻辑

秒差不应简单按当前视频时间比较，而应按“当前圈内距离进度”对齐。推荐流程：

1. 对每圈建立 `lap_distance_m → lap_elapsed_sec` 的插值函数；
2. 当前圈在某一帧的距离为 `d_current`；
3. 查询最快圈在同一距离 `d_current` 处的耗时 `t_best(d_current)`；
4. 当前圈耗时为 `t_current(d_current)`；
5. 秒差为：

```text
delta = t_current(d_current) - t_best(d_current)
```

显示规则：

- `+0.324s`：当前圈比最快圈慢；
- `-0.112s`：当前圈比最快圈快；
- `--`：当前圈还没有足够数据，或者参考圈无效。

---

## 5.8 数据模块/图层模块

### 5.8.1 模块总览

软件首期需要内置以下模块：

| 模块 | 必须实现 | 可配置项 |
|---|---:|---|
| 赛道路线示意图 + 当前车位置箭头 | 是 | 大小、线宽、颜色、箭头样式、是否显示完整路线 |
| 当前车速 | 是 | 单位 km/h/mph、字体、颜色、小数位 |
| G 力球 | 是 | 量程、网格、历史拖尾、横纵轴方向 |
| 当前计时 | 是 | 当前圈计时/全局计时/数据计时 |
| 与最快圈秒差 | 是 | 显示正负号、颜色规则、参考圈 |
| 分段计时 | 是 | 显示数量、刷紫规则、排序方式 |
| 前后五圈成绩表 | 是 | 当前圈居中、显示差值、是否显示有效圈 |
| LOGO/自定义图标 | 后续 | 图片导入、透明度、缩放 |
| 油门/刹车条 | 后续 | 依赖数据源是否有通道 |
| 方向盘角度 | 后续 | 依赖数据源是否有通道 |

### 5.8.2 赛道路线示意图模块

功能要求：

1. 显示完整赛道轨迹；
2. 当前车位置用三角箭头表示；
3. 三角箭头朝向应反映车头方向；
4. 若数据有原始 heading，则优先使用原始 heading；
5. 若没有 heading，则用相邻轨迹点计算航向角；
6. 支持显示当前圈轨迹高亮；
7. 支持显示最快圈轨迹作为参考；
8. 支持开启/关闭起终点线和分段线显示；
9. 支持地图自动适配模块边界；
10. 支持固定北向上或车头向上两种模式。

### 5.8.3 当前车速模块

功能要求：

1. 显示当前速度；
2. 支持 km/h 和 mph；
3. 支持整数和一位小数；
4. 支持数字仪表样式和普通文字样式；
5. 当速度为估算值时，可在设置中选择是否显示估算标识。

### 5.8.4 G 力球模块

功能要求：

1. 横轴显示横向 G；
2. 纵轴显示纵向 G；
3. 支持显示当前 G 点；
4. 支持显示最近 0.5–3 秒拖尾；
5. 支持设置量程，例如 ±1.5G、±2G、±3G；
6. 支持网格线显示；
7. 支持当 G 力为估算值时显示低置信度标识。

注意：如果输入数据只有 GPX，经纬度和时间只能估算速度变化与转弯曲率，无法得到与 IMU 等价的真实 G 力。此时应在 UI 中提示“G 力为轨迹估算值”。

### 5.8.5 当前计时模块

支持三种计时模式：

| 模式 | 说明 |
|---|---|
| 当前圈计时 | 从本圈起点线通过时刻开始 |
| 全局数据计时 | 从 GPS/VBO 数据起点开始 |
| 视频同步计时 | 从用户设定的视频同步点开始 |

### 5.8.6 与最快圈秒差模块

功能要求：

1. 显示当前圈与参考圈在同一圈内距离进度下的秒差；
2. 支持参考圈选择：最快圈、指定圈、理论最佳圈；
3. 支持颜色规则：
   - 慢于参考圈：红色；
   - 快于参考圈：绿色；
   - 无数据：灰色；
4. 支持平滑显示，避免数值抖动过强。

### 5.8.7 分段计时模块

功能要求：

1. 显示当前圈所有分段；
2. 当前正在进行的分段高亮；
3. 已完成分段显示本圈分段成绩；
4. 未完成分段显示 `--`；
5. 刷新本场最佳分段时显示紫色；
6. 支持显示分段差值，例如 `+0.123`、`-0.045`；
7. 支持分段名称自定义。

### 5.8.8 前后五圈成绩模块

功能要求：

1. 以当前圈为中心显示五圈成绩；
2. 当前圈上方显示前两圈；
3. 当前圈下方显示后两圈；
4. 当前圈高亮；
5. 若当前圈不足前后两圈，则自动补空；
6. 支持显示每圈与最快圈差值；
7. 支持标记最快圈；
8. 支持无效圈灰显。

显示示例：

```text
Lap 04   47.982   +0.410
Lap 05   47.655   +0.083
Lap 06   47.572   BEST
Lap 07   47.901   +0.329
Lap 08   48.230   +0.658
```

---

## 5.9 图层画布编辑器

### 功能描述

透明画布编辑器用于决定最终导出图层中各模块的位置、尺寸和样式。

### 功能要求

1. 画布背景以透明棋盘格显示；
2. 可以切换显示视频预览作为参考背景；
3. 所有模块可以勾选显示/隐藏；
4. 所有模块可以拖动位置；
5. 所有模块可以缩放尺寸；
6. 模块缩放逻辑类似 Windows 窗口，支持拖拽边缘和角点；
7. 支持模块锁定，防止误拖动；
8. 支持吸附到画布边缘；
9. 支持网格吸附；
10. 支持模块层级调整；
11. 支持复制模块样式；
12. 支持保存布局预设；
13. 支持加载布局预设；
14. 支持恢复默认布局。

### 模块公共属性

| 属性 | 说明 |
|---|---|
| `visible` | 是否显示 |
| `x` | 左上角 x 坐标 |
| `y` | 左上角 y 坐标 |
| `width` | 模块宽度 |
| `height` | 模块高度 |
| `opacity` | 透明度 |
| `z_index` | 图层层级 |
| `locked` | 是否锁定 |
| `style` | 样式配置 |
| `data_source` | 绑定数据源 |

---

## 5.10 视频与数据同步

### 功能描述

由于视频和 GPS/VBO 文件的起始时间可能不同，软件需要提供手动对齐能力。

### 同步方式

#### 方式 A：视频帧与轨迹点手动对齐

1. 用户在视频预览中暂停到某一帧；
2. 用户在轨迹地图上点击对应车辆位置；
3. 软件计算：

```text
sync_offset = video_time_sec - data_elapsed_sec
```

4. 之后任意视频时间 `t_video` 对应的数据时间为：

```text
t_data = t_video - sync_offset
```

#### 方式 B：输入具体偏移量

用户可以直接输入偏移量，例如：

```text
视频时间 00:34.520 对应 GPS 数据 00:00.000
```

#### 方式 C：多点对齐校验

高级模式下允许用户选择两个或多个对应点，用于检查是否存在视频变速、VFR 可变帧率或数据时间漂移问题。

### 预览要求

1. 同步界面必须支持低清视频预览；
2. 预览中可以显示当前遥测模块；
3. 用户拖动时间轴时，地图和图层模块同步刷新；
4. 若当前视频时间超出数据范围，模块显示为空或灰色；
5. 对齐完成后显示导出起点信息。

### 导出起点信息

导出时需要明确显示：

```text
请将导出的透明图层放置到原视频时间轴的 00:34.520 处。
透明图层数据长度：05:12.850。
```

该信息同时写入导出 JSON。

---

## 5.11 导出透明图层

### 功能描述

软件导出一个不包含原视频画面的透明背景动态数据图层。

### 导出要求

1. 默认导出画布尺寸等于原视频；
2. 背景透明；
3. 只渲染用户勾选的数据模块；
4. 导出时长默认覆盖全部 GPX/VBO 数据点；
5. 用户可选择导出完整数据时长或指定时间范围；
6. 支持导出进度显示；
7. 支持导出中断；
8. 支持导出日志；
9. 支持导出后自动打开文件夹。

### 导出流程建议

1. 根据导出帧率生成每一帧时间戳；
2. 根据时间戳查询遥测数据插值结果；
3. 使用透明 RGBA 画布渲染所有模块；
4. 将 RGBA 帧写入临时 PNG 序列或管道；
5. 调用 FFmpeg 编码为带 Alpha 的视频；
6. 生成导出 JSON；
7. 清理临时文件或询问是否保留。

### 推荐导出预设

| 预设 | 参数 | 用途 |
|---|---|---|
| 剪辑软件优先 | MOV ProRes 4444 | Premiere/DaVinci 常用 |
| 最大兼容 | PNG 序列 | 所有剪辑软件兜底 |
| 小体积 | WebM VP9 Alpha | 体积敏感场景 |

---

## 6. 扩展能力设计

### 6.1 插件式图层模块

为了保留后续新增图标和图表的能力，图层模块不应写成固定函数，而应设计成插件式结构。

### 6.2 模块接口建议

每个图层模块至少实现以下接口：

```python
class OverlayWidgetBase:
    widget_id: str
    widget_name: str
    default_config: dict

    def get_config_schema(self) -> dict:
        pass

    def update_data(self, telemetry_frame: dict, context: dict) -> None:
        pass

    def render(self, painter, rect, telemetry_frame: dict, context: dict) -> None:
        pass

    def serialize(self) -> dict:
        pass

    def deserialize(self, data: dict) -> None:
        pass
```

### 6.3 后续可扩展模块

| 模块 | 数据依赖 | 备注 |
|---|---|---|
| 油门开度 | VBO/CAN/OBD | GPX 通常没有 |
| 刹车压力 | VBO/CAN/OBD | 需要原始数据支持 |
| 方向盘角度 | CAN/专用设备 | 非通用数据 |
| 转速 | OBD/CAN | 可作为后续扩展 |
| 档位 | OBD/CAN/手动计算 | 卡丁车通常不需要 |
| 理论最佳圈 | 圈速和分段数据 | 适合后续实现 |
| 线路对比图 | 多圈轨迹数据 | 适合复盘视频 |
| 速度曲线 | 距离-速度数据 | 信息密度较高 |

---

## 7. 技术方案建议

## 7.1 总体架构

推荐采用分层架构：

```text
Desktop UI Layer
    ↓
Project & Configuration Layer
    ↓
Telemetry Parsing Layer
    ↓
Track / Timing / Lap Analysis Layer
    ↓
Overlay Widget Engine
    ↓
Preview Engine / Export Render Engine
    ↓
FFmpeg Encoding Layer
```

### 各层职责

| 层级 | 职责 |
|---|---|
| UI 层 | Qt 界面、拖拽、窗口布局、用户交互 |
| 工程层 | 保存工程配置、素材路径、图层布局 |
| 数据解析层 | GPX/VBO 解析和标准化 |
| 计时分析层 | 起终点、分段、圈速、秒差、插值 |
| 图层引擎 | 每个模块的数据绑定和绘制 |
| 预览引擎 | 低清视频预览和透明图层预览 |
| 导出引擎 | 逐帧 RGBA 渲染和 FFmpeg 编码 |

---

## 7.2 技术选型

### 7.2.1 GUI 框架

| 方案 | 优点 | 缺点 | 建议 |
|---|---|---|---|
| PyQt5 | 成熟、资料多、符合 Qt5 要求 | 授权需要注意 GPL/商业授权 | 首期可用 |
| PySide2 | Qt5 官方 Python 绑定，LGPL | 生态资料略少于 PyQt5 | 可替代 PyQt5 |
| PySide6 | 长期维护更好 | 与“Qt5”要求不完全一致 | 后续升级考虑 |

建议：首期采用 **PyQt5 或 PySide2 + Qt Widgets + QGraphicsView/QGraphicsScene**。不建议使用 Electron、Web 前端或浏览器内嵌网页作为主界面。

### 7.2.2 视频处理

| 功能 | 推荐工具 |
|---|---|
| 视频信息读取 | FFprobe |
| 视频解码预览 | OpenCV / PyAV / FFmpeg 抽帧 |
| 最终透明视频编码 | FFmpeg |
| Alpha 透明兜底 | PNG 序列 |

不建议完全依赖 Qt Multimedia 来解决所有视频兼容问题，因为桌面端多格式兼容更适合交给 FFmpeg。

### 7.2.3 地理坐标与轨迹计算

| 功能 | 推荐工具/方法 |
|---|---|
| GPX 解析 | gpxpy / lxml |
| VBO 解析 | 自研解析器 + 通道映射表 |
| 经纬度转局部平面坐标 | pyproj / 局部 ENU 近似 |
| 距离计算 | haversine / pyproj Geod |
| 插值 | numpy / scipy |
| 数据表处理 | pandas |

### 7.2.4 地图与卫星底图

可选方案：

| 方案 | 说明 | 建议 |
|---|---|---|
| 本地瓦片缓存 | 下载/缓存卫星图瓦片后用 Qt 绘制 | 推荐 |
| MBTiles | 离线地图瓦片数据库 | 推荐中后期实现 |
| 在线 XYZ 瓦片 | 从地图服务拉取瓦片 | 需注意授权与使用限制 |
| QWebEngine + Leaflet | 本质接近网页方案 | 不推荐，除非后续快速验证 |

注意：卫星图服务通常涉及授权和调用限制。首期可以先实现“无底图轨迹编辑 + 可选本地图片底图导入”，再扩展在线卫星瓦片。

### 7.2.5 打包发布

| 方案 | 优点 | 缺点 |
|---|---|---|
| PyInstaller | Python 桌面应用常用，Windows 打包方便 | 包体较大，Qt/FFmpeg 路径需处理 |
| Nuitka | 性能和封装效果可能更好 | 配置复杂度更高 |
| cx_Freeze | 可用 | 生态热度一般 |

首期建议使用 PyInstaller，并将 FFmpeg/FFprobe 作为随包二进制文件一起分发。

---

## 8. 页面与交互设计

## 8.1 主界面布局

建议主界面分为五个区域：

```text
┌──────────────────────────────────────────────┐
│ 顶部菜单：文件 / 数据 / 地图 / 图层 / 导出 / 帮助 │
├───────────────┬──────────────────────┬───────┤
│ 左侧素材与模块 │ 中央预览/地图/画布区域 │ 右侧属性 │
│ 工程树         │ 视频预览 / 地图编辑     │ 参数面板 │
│ 数据文件       │ 透明画布编辑           │ 样式配置 │
│ 模块列表       │                      │         │
├───────────────┴──────────────────────┴───────┤
│ 底部时间轴：视频时间 / 数据时间 / 圈号 / 分段      │
└──────────────────────────────────────────────┘
```

## 8.2 推荐工作流

### 第一步：导入素材

1. 新建工程；
2. 导入视频；
3. 导入 GPX/VBO；
4. 查看数据质量报告。

### 第二步：设置赛道

1. 打开地图编辑页；
2. 查看轨迹；
3. 可选加载卫星底图；
4. 校准轨迹显示位置；
5. 设置起终点线；
6. 设置分段线；
7. 检查圈速表是否合理。

### 第三步：同步视频与数据

1. 打开同步页；
2. 视频暂停到参考画面；
3. 地图选择对应 GPS 点；
4. 软件计算偏移；
5. 低清预览检查同步是否合理。

### 第四步：编辑透明图层

1. 打开画布编辑页；
2. 勾选需要的模块；
3. 拖动模块位置；
4. 调整模块尺寸；
5. 设置字体、颜色、线宽、透明度；
6. 保存布局预设。

### 第五步：导出

1. 选择导出格式；
2. 选择导出帧率；
3. 选择导出范围；
4. 开始导出；
5. 获取透明图层文件和 JSON 对齐说明；
6. 在剪辑软件中放置到指定视频时间点。

---

## 9. 计算逻辑细化

## 9.1 经纬度转本地坐标

为便于计算距离、穿线、轨迹绘制，需要将经纬度转换为局部平面坐标。首期可以采用以轨迹第一个点或轨迹中心点为原点的局部坐标系。

推荐字段：

```text
origin_lat = mean(lat)
origin_lon = mean(lon)
x_m = east displacement from origin
y_m = north displacement from origin
```

对于卡丁车赛道这种小范围场景，局部平面近似通常足够；若后续扩展到长距离道路测试，应使用更严格的投影转换。

## 9.2 速度计算

若数据文件包含速度字段：

```text
speed_kmh = raw_speed
```

若缺失速度字段：

```text
speed_mps = distance(point_i, point_i-1) / time_delta
speed_kmh = speed_mps * 3.6
```

需要对速度做平滑，避免 GPS 噪声导致速度抖动。

## 9.3 航向计算

若数据文件包含 heading/course：

```text
heading_deg = raw_heading
```

若缺失航向：

```text
heading_deg = bearing(point_i, point_i+1)
```

低速或停车时，航向角容易抖动。应采用上一有效航向或窗口平滑。

## 9.4 G 力估算

若 VBO 提供横向/纵向 G，应直接使用原始通道。

若只有 GPX，则只能估算：

```text
longitudinal_accel = dv / dt
lateral_accel ≈ v² * curvature
G = acceleration / 9.80665
```

限制：GPX 估算 G 力对采样率、GPS 噪声、平滑窗口非常敏感，不能等价于 IMU 实测值。UI 和导出时应允许标注“Estimated”。

## 9.5 分段线穿越检测

将车辆相邻两个轨迹点组成线段，将起终点线/分段线也视为线段。若两条线段相交，则认为车辆穿越检测线。

需要额外检查：

1. 是否满足通过方向；
2. 是否超过冷却时间；
3. 是否超过冷却距离；
4. 当前速度是否超过最低阈值；
5. 是否为有效圈。

## 9.6 插值策略

渲染视频时，导出帧率通常高于数据采样率。例如 GPS 是 10 Hz，视频是 60 fps。因此每个导出帧都需要对遥测数据插值。

推荐插值字段：

| 字段 | 插值方式 |
|---|---|
| 位置 x/y | 线性插值 |
| 速度 | 线性插值 + 平滑 |
| 航向 | 角度插值，处理 0/360 跳变 |
| G 力 | 线性插值 + 平滑 |
| 计时 | 直接按时间计算 |
| 圈号/分段号 | 阶跃值，不线性插值 |

---

## 10. 非功能需求

## 10.1 性能需求

| 项目 | 目标 |
|---|---|
| 10 Hz、30 分钟 GPX 导入 | 5 秒内完成解析 |
| 地图拖动缩放 | 基本流畅，无明显卡死 |
| 1080p 低清预览 | 可降采样到 720p 或更低 |
| 1080p 60fps 透明图层导出 | 允许离线渲染，不要求实时 |
| 内存占用 | 普通项目建议小于 2 GB |

## 10.2 稳定性需求

1. 单个数据点异常不应导致软件崩溃；
2. 视频解码失败应给出明确错误信息；
3. 数据字段缺失应允许用户手动映射或降级使用；
4. 导出失败应保留日志；
5. 中途取消导出应清理临时文件；
6. 工程保存应避免损坏，建议采用临时文件写入后替换。

## 10.3 兼容性需求

1. Windows 10/11 优先支持；
2. Windows 8.1 可作为尽力兼容；
3. 暂不承诺 macOS/Linux；
4. 视频解码由随包 FFmpeg 提供；
5. 不要求用户本机安装 Python；
6. 不要求用户安装 FFmpeg。

## 10.4 可维护性需求

1. 数据解析、计时计算、图层渲染应分模块；
2. 不允许把全部逻辑写进 Qt 界面类；
3. 需要保存导出日志；
4. 需要单元测试覆盖关键计算逻辑；
5. 需要准备样例 GPX/VBO/视频用于回归测试。

---

## 11. 异常与边界情况

| 异常情况 | 处理方式 |
|---|---|
| GPX 没有时间戳 | 无法计算圈速，提示用户该文件不适合计时分析 |
| GPX 没有速度 | 根据位置和时间估算速度 |
| GPX 没有 G 力 | 估算或隐藏 G 力模块 |
| VBO 通道名不标准 | 弹出通道映射窗口 |
| GPS 轨迹漂移 | 支持视觉对齐，但保留原始计算数据 |
| 起点线设置错误 | 圈速表异常时提示检查起点线方向和位置 |
| 视频短于数据 | 导出仍覆盖数据，但提示剪辑软件中可能超出原视频范围 |
| 数据短于视频 | 超出数据范围后模块显示为空/灰色 |
| 可变帧率视频 | 提示可能影响同步，建议导出前转恒定帧率 |
| 分段线顺序错误 | 支持手动排序 |
| 低采样率 GPS | 提示计时精度有限 |
| 多条轨迹段 | 允许选择主轨迹段或合并轨迹段 |
| 地图瓦片加载失败 | 回退到无底图轨迹显示 |

---

## 12. 版本规划

## 12.1 MVP 版本：能跑通完整流程

目标：完成从 GPX + 视频到透明图层导出的闭环。

功能范围：

1. 导入视频；
2. 读取视频尺寸和帧率；
3. 导入 GPX；
4. 轨迹显示；
5. 手动设置起终点线；
6. 手动设置分段线；
7. 圈速和分段计算；
8. 最快圈识别；
9. 手动同步视频与数据；
10. 基础透明画布；
11. 内置七个核心模块；
12. 模块勾选、拖动、缩放；
13. PNG 序列导出；
14. MOV ProRes 4444 导出；
15. 工程保存。

## 12.2 V1.1：增强数据与地图能力

功能范围：

1. VBO 导入；
2. 通道映射窗口；
3. 卫星底图；
4. 轨迹平移/旋转/缩放校准；
5. 低清视频预览优化；
6. 秒差曲线平滑；
7. 分段刷紫规则完善；
8. 导出日志面板。

## 12.3 V1.2：增强图层和模板

功能范围：

1. 布局模板；
2. 样式主题；
3. 自定义 LOGO/图标模块；
4. 速度曲线模块；
5. 线路对比模块；
6. 理论最佳圈模块；
7. 批量导出。

## 12.4 V2.0：专业遥测分析扩展

功能范围：

1. 多圈轨迹对比；
2. 多数据源融合；
3. CAN/OBD 数据导入；
4. 油门/刹车/转速/方向盘角度显示；
5. 数据分析报告导出；
6. 自动弯角识别；
7. 自动生成视频片段建议。

---

## 13. 开发任务拆分

## 13.1 模块拆分

| 模块 | 主要任务 | 优先级 |
|---|---|---:|
| 项目管理 | 工程新建/保存/打开 | P0 |
| 视频信息读取 | FFprobe 解析视频元数据 | P0 |
| GPX 解析 | 经纬度、时间、速度估算 | P0 |
| VBO 解析 | 通道读取、字段映射 | P1 |
| 坐标转换 | 经纬度转局部坐标 | P0 |
| 地图编辑 | 轨迹显示、起点线、分段线 | P0 |
| 圈速引擎 | 穿线检测、圈速、分段 | P0 |
| 秒差引擎 | 当前圈 vs 最快圈 | P0 |
| 同步引擎 | 视频时间与数据时间偏移 | P0 |
| 画布编辑器 | 模块拖动、缩放、锁定 | P0 |
| 图层模块 | 七个核心模块 | P0 |
| 预览引擎 | 低清视频 + 图层预览 | P1 |
| 导出引擎 | RGBA 渲染、PNG/MOV 输出 | P0 |
| 打包发布 | PyInstaller、依赖收集 | P1 |
| 测试体系 | 单元测试、样例数据、导出回归 | P0 |

## 13.2 推荐代码目录结构

```text
kart_overlay/
  app.py
  ui/
    main_window.py
    map_editor.py
    canvas_editor.py
    sync_panel.py
    export_dialog.py
  core/
    project.py
    telemetry_model.py
    timebase.py
  parsers/
    gpx_parser.py
    vbo_parser.py
    channel_mapper.py
  geo/
    projection.py
    distance.py
    line_crossing.py
  analysis/
    lap_detector.py
    sector_detector.py
    delta_calculator.py
    smoothing.py
  overlay/
    base_widget.py
    track_map_widget.py
    speed_widget.py
    g_ball_widget.py
    timer_widget.py
    delta_widget.py
    sector_widget.py
    lap_table_widget.py
  render/
    preview_renderer.py
    frame_renderer.py
    ffmpeg_exporter.py
  video/
    ffprobe.py
    frame_reader.py
  resources/
    themes/
    icons/
  tests/
    test_lap_detector.py
    test_sector_detector.py
    test_delta_calculator.py
```

---

## 14. 验收标准

## 14.1 功能验收

| 编号 | 验收项 | 标准 |
|---|---|---|
| A01 | 导入视频 | 能读取分辨率、帧率、时长 |
| A02 | 导入 GPX | 能显示轨迹并生成数据质量报告 |
| A03 | 导入 VBO | 能识别通道并生成标准数据表 |
| A04 | 起点线 | 用户可画线并计算圈速 |
| A05 | 分段线 | 用户可画多个分段并计算分段成绩 |
| A06 | 最快圈 | 能自动识别最快有效圈 |
| A07 | 秒差 | 能显示当前圈相对最快圈秒差 |
| A08 | 画布模块 | 七个核心模块均可显示/隐藏 |
| A09 | 拖动缩放 | 模块可拖动、可缩放、可锁定 |
| A10 | 同步 | 可通过视频帧和数据点完成同步 |
| A11 | 预览 | 能低清预览图层与视频是否同步 |
| A12 | 导出 | 能导出透明 PNG 序列 |
| A13 | 导出 | 能导出透明 MOV 文件 |
| A14 | 对齐信息 | 导出 JSON 包含视频起始时间信息 |
| A15 | 工程保存 | 关闭后重新打开可恢复配置 |

## 14.2 计时精度验收

计时精度取决于数据采样率。验收时不应笼统要求绝对精确，而应按数据频率设定合理阈值。

| 数据频率 | 预期计时误差 |
|---|---|
| 1 Hz GPX | 误差可能较大，仅适合路线展示 |
| 5 Hz GPX | 约 ±0.2–0.5 s，取决于速度和插值 |
| 10 Hz GPX/VBO | 约 ±0.1–0.2 s |
| 20 Hz+ VBO | 可进一步降低误差 |

软件应在数据质量报告中显示“理论计时精度提示”。

## 14.3 导出验收

1. 导出的 MOV/PNG 背景必须透明；
2. 导出画布尺寸与原视频一致；
3. 将透明图层放到 JSON 指定的视频时间点后，车辆位置和数据变化应基本同步；
4. 当前速度、计时、圈号、分段成绩应随时间正确变化；
5. 未勾选模块不应出现在导出文件中。

---

## 15. 风险与应对策略

| 风险 | 影响 | 应对策略 |
|---|---|---|
| GPX 数据缺少速度/G 力 | 模块无法真实显示 | 估算并标记低置信度，允许隐藏模块 |
| GPS 采样率低 | 圈速/秒差不准 | 数据质量报告提示，插值但不夸大精度 |
| VBO 通道命名不统一 | 解析失败 | 做通道映射窗口和别名表 |
| 视频格式复杂 | 预览/读取失败 | 使用 FFmpeg/FFprobe，Qt 只负责 UI |
| Alpha 视频兼容性 | 剪辑软件导入异常 | 提供 PNG 序列兜底 |
| 卫星地图授权问题 | 商业/发布风险 | 优先本地瓦片/用户自备底图，明确地图服务配置 |
| 画布交互复杂 | 开发周期变长 | MVP 先实现拖动缩放锁定，后续再做吸附/参考线 |
| 导出速度慢 | 使用体验下降 | 先保证正确性，后续优化管道渲染和并行 |
| PyInstaller 打包体积大 | 分发不便 | 接受首期体积，后续再做精简 |

---

## 16. 产品决策建议

### 16.1 首期必须坚持的设计原则

1. **先闭环，再精致**：先实现导入、计算、布局、透明导出的完整闭环。
2. **计算与显示分离**：轨迹视觉对齐不能破坏原始 GPS 计时计算。
3. **预览与导出分离**：预览可以低清低帧率，导出必须按目标画质重新渲染。
4. **真实值与估算值分离**：GPX 估算 G 力必须标注，不能伪装成 IMU 实测。
5. **图层模块插件化**：为后续新增仪表、图标、曲线保留接口。
6. **透明图层优先**：不要首期就做原视频压制，否则会增加编码复杂度并降低后期自由度。

### 16.2 推荐 MVP 最小功能清单

如果需要尽快开始开发，最小可行版本建议只做以下内容：

1. 视频导入并读取画布尺寸；
2. GPX 导入；
3. 轨迹地图显示；
4. 起终点线；
5. 分段线；
6. 圈速/分段/最快圈；
7. 视频-GPX 手动偏移；
8. 赛道小地图、速度、计时、秒差、分段表；
9. 模块拖动和缩放；
10. PNG 序列导出；
11. MOV ProRes 4444 导出。

VBO、卫星底图、轨迹旋转缩放校准、模板系统可以排在第二阶段，但接口应在第一阶段预留。

---

## 17. 参考依据

1. GPX 1.1 Schema Documentation, Topografix: https://www.topografix.com/gpx/1/1/
2. Racelogic .VBO Files Knowledge Base: https://en.racelogic.support/Knowledge_Base/.VBO_Files
3. Racelogic File Formats Overview: https://en.racelogic.support/Knowledge_Base/File_Formats
4. FFmpeg Formats Documentation: https://ffmpeg.org/ffmpeg-formats.html
5. PyInstaller Manual: https://www.pyinstaller.org/
6. PyInstaller Supported Platforms / Requirements: https://github.com/pyinstaller/pyinstaller
7. Qt for Python QtWidgets / QGraphicsView Documentation: https://doc.qt.io/qtforpython-5/PySide2/QtWidgets/QGraphicsView.html
8. OpenStreetMap Tile Usage Policy: https://operations.osmfoundation.org/policies/tiles/

---

## 18. 下一步建议

建议下一步进入“技术原型阶段”，不要直接写完整软件。原型阶段只验证四件事：

1. 能否稳定读取 GPX/VBO 并转成标准数据表；
2. 起终点线和分段线穿越检测是否可靠；
3. Qt 透明画布模块拖动缩放是否顺手；
4. FFmpeg 是否能稳定导出剪辑软件可识别的透明图层。

只要这四件事验证成功，后续才进入完整 UI 和工程化开发。否则会出现界面做得很漂亮，但核心计时和导出不可用的问题。
